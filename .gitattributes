# 줄바꿈이 실행 가능 여부를 좌우하는 파일만 고정한다.
# (저장소 대부분은 CRLF이므로 전역 정규화는 하지 않는다 — diff가 통째로 부풀어 리뷰가 불가능해진다.)

# 셸 스크립트는 리눅스에서 `curl | bash`로 실행된다.
# CRLF로 체크아웃되면 셔뱅이 `bash\r`로 읽혀 설치 자체가 실패한다.
*.sh text eol=lf

# 반대로 cmd.exe는 CRLF가 아니면 라벨과 명령을 잘못 파싱한다.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
